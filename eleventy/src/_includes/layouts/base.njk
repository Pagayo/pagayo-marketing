<!DOCTYPE html>
<html lang="{{ country.lang }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title | default("Pagayo - Multi-Tenant E-commerce Platform") }}</title>
  <meta name="description" content="{{ description | default("Bouw en schaal moderne webshops, hotels, en POS systemen op één krachtig multi-tenant platform.") }}">
  
  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  
  <!-- Styles -->
  <link rel="stylesheet" href="/styles/global.css">
  
  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
</head>
<body>
  {% include "partials/header.njk" %}
  
  <main class="page-container">
    {{ content | safe }}
  </main>
  
  {% include "partials/footer.njk" %}
</body>
</html>
