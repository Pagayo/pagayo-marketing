---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const englishCountries = [
  { name: 'Verenigde Staten', code: 'us', flag: '🇺🇸', language: 'Engels' },
  { name: 'Canada', code: 'ca', flag: '🇨🇦', language: 'Engels' },
  { name: 'Jamaica', code: 'jm', flag: '🇯🇲', language: 'Engels' },
  { name: 'Trinidad en Tobago', code: 'tt', flag: '🇹🇹', language: 'Engels' },
  { name: 'Belize', code: 'bz', flag: '🇧🇿', language: 'Engels' },
  { name: 'Guyana', code: 'gy', flag: '🇬🇾', language: 'Engels' },
  { name: 'Bahamas', code: 'bs', flag: '🇧🇸', language: 'Engels' },
  { name: 'Barbados', code: 'bb', flag: '🇧🇧', language: 'Engels' },
  { name: 'Grenada', code: 'gd', flag: '🇬🇩', language: 'Engels' },
  { name: 'Saint Lucia', code: 'lc', flag: '🇱🇨', language: 'Engels' },
  { name: 'Saint Vincent en de Grenadines', code: 'vc', flag: '🇻🇨', language: 'Engels' },
  { name: 'Antigua en Barbuda', code: 'ag', flag: '🇦🇬', language: 'Engels' },
  { name: 'Dominica', code: 'dm', flag: '🇩🇲', language: 'Engels' },
  { name: 'Saint Kitts en Nevis', code: 'kn', flag: '🇰🇳', language: 'Engels' },
];

const spanishCountries = [
  { name: 'Mexico', code: 'mx', flag: '🇲🇽', language: 'Spaans' },
  { name: 'Argentinië', code: 'ar', flag: '🇦🇷', language: 'Spaans' },
  { name: 'Chili', code: 'cl', flag: '🇨🇱', language: 'Spaans' },
  { name: 'Colombia', code: 'co', flag: '🇨🇴', language: 'Spaans' },
  { name: 'Costa Rica', code: 'cr', flag: '🇨🇷', language: 'Spaans' },
  { name: 'Ecuador', code: 'ec', flag: '🇪🇨', language: 'Spaans' },
  { name: 'El Salvador', code: 'sv', flag: '🇸🇻', language: 'Spaans' },
  { name: 'Guatemala', code: 'gt', flag: '🇬🇹', language: 'Spaans' },
  { name: 'Honduras', code: 'hn', flag: '🇭🇳', language: 'Spaans' },
  { name: 'Panama', code: 'pa', flag: '🇵🇦', language: 'Spaans' },
  { name: 'Paraguay', code: 'py', flag: '🇵🇾', language: 'Spaans' },
  { name: 'Peru', code: 'pe', flag: '🇵🇪', language: 'Spaans' },
  { name: 'Uruguay', code: 'uy', flag: '🇺🇾', language: 'Spaans' },
  { name: 'Bolivia', code: 'bo', flag: '🇧🇴', language: 'Spaans' },
  { name: 'Dominicaanse Republiek', code: 'do', flag: '🇩🇴', language: 'Spaans' },
];

const portugueseCountries = [
  { name: 'Brazilië', code: 'br', flag: '🇧🇷', language: 'Portugees' },
];

const otherLanguageCountries = [
  { name: 'Suriname', code: 'sr', flag: '🇸🇷', language: 'Nederlands' },
  { name: 'Haïti', code: 'ht', flag: '🇭🇹', language: 'Frans/Creools' },
  { name: 'Frans-Guyana', code: 'gf', flag: '🇬🇫', language: 'Frans' },
];
---

<BaseLayout title="Pagayo Amerika - E-commerce Platform" description="Ontdek Pagayo e-commerce diensten in alle Amerikaanse landen" lang="nl">
  <Header />

  <main class="container">
    <section class="section">
      <div class="section-header">
        <h1 class="section-title">Pagayo in Amerika</h1>
        <p class="section-subtitle">
          Wij zijn actief in Amerikaanse landen waar we lokale betaalmethoden, 
          verzendopties en geoptimaliseerde checkout flows aanbieden voor jouw webshop.
        </p>
      </div>

      <!-- Engelstalige landen -->
      <div class="language-section">
        <h2 class="language-title">Engelstalige landen</h2>
        <div class="countries-grid">
          {englishCountries.map(country => (
            <a href={`/${country.code}/`} class="country-card">
              <div class="country-flag">{country.flag}</div>
              <h3 class="country-name">{country.name}</h3>
              <p class="country-language">{country.language}</p>
              <div class="country-arrow">→</div>
            </a>
          ))}
        </div>
      </div>

      <!-- Spaanstalige landen -->
      <div class="language-section">
        <h2 class="language-title">Spaanstalige landen</h2>
        <div class="countries-grid">
          {spanishCountries.map(country => (
            <a href={`/${country.code}/`} class="country-card">
              <div class="country-flag">{country.flag}</div>
              <h3 class="country-name">{country.name}</h3>
              <p class="country-language">{country.language}</p>
              <div class="country-arrow">→</div>
            </a>
          ))}
        </div>
      </div>

      <!-- Portugeestalige landen -->
      <div class="language-section">
        <h2 class="language-title">Portugeestalige landen</h2>
        <div class="countries-grid">
          {portugueseCountries.map(country => (
            <a href={`/${country.code}/`} class="country-card">
              <div class="country-flag">{country.flag}</div>
              <h3 class="country-name">{country.name}</h3>
              <p class="country-language">{country.language}</p>
              <div class="country-arrow">→</div>
            </a>
          ))}
        </div>
      </div>

      <!-- Andere talen -->
      <div class="language-section">
        <h2 class="language-title">Andere talen</h2>
        <div class="countries-grid">
          {otherLanguageCountries.map(country => (
            <a href={`/${country.code}/`} class="country-card">
              <div class="country-flag">{country.flag}</div>
              <h3 class="country-name">{country.name}</h3>
              <p class="country-language">{country.language}</p>
              <div class="country-arrow">→</div>
            </a>
          ))}
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .section {
    padding: 6rem 2rem;
  }

  .language-section {
    margin-bottom: 4rem;
  }

  .language-section:last-child {
    margin-bottom: 0;
  }

  .language-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 2rem;
    text-align: center;
  }

  .section-header {
    max-width: 700px;
    margin: 0 auto 4rem;
    text-align: center;
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 800;
    margin-bottom: 1rem;
    color: var(--text);
  }

  .section-subtitle {
    font-size: 1.15rem;
    color: var(--text-secondary);
    line-height: 1.6;
  }

  .countries-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .country-card {
    background: var(--bg-secondary);
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid var(--border);
    transition: all 0.3s ease;
    text-decoration: none;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .country-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0, 255, 136, 0.05) 0%, transparent 100%);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .country-card:hover {
    border-color: var(--accent);
    transform: translateY(-6px);
    box-shadow: 0 12px 32px rgba(0, 255, 136, 0.15);
  }

  .country-card:hover::before {
    opacity: 1;
  }

  .country-flag {
    font-size: 3rem;
    margin-bottom: 1rem;
    transition: transform 0.3s ease;
  }

  .country-card:hover .country-flag {
    transform: scale(1.1);
  }

  .country-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--text);
    margin-bottom: 0.5rem;
  }

  .country-language {
    font-size: 0.85rem;
    color: var(--accent);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .country-arrow {
    font-size: 1.5rem;
    color: var(--accent);
    opacity: 0;
    transform: translateX(-10px);
    transition: all 0.3s ease;
    margin-top: auto;
  }

  .country-card:hover .country-arrow {
    opacity: 1;
    transform: translateX(0);
  }

  /* RESPONSIVE */
  @media (max-width: 1024px) {
    .countries-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .section {
      padding: 4rem 1rem;
    }

    .countries-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
    }

    .country-card {
      padding: 1.5rem;
    }

    .country-flag {
      font-size: 2.5rem;
    }

    .country-name {
      font-size: 1.1rem;
    }
  }

  @media (max-width: 480px) {
    .countries-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
