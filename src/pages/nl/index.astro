---
import MarketingLayout from '../../layouts/MarketingLayout.astro';
---

<MarketingLayout
  title="Pagayo - Multi-Tenant E-commerce Platform"
  description="Bouw en schaal moderne webshops, hotels, en POS systemen op één krachtig multi-tenant platform."
  lang="nl"
  headerVariant="transparent"
>

<style>
  /* Pagayo Brand System */
  :root {
    --primary: #2d1b69;
    --primary-dark: #1f1348;
    --accent: #ff7a59;
    --accent-hover: #e05f3f;
    --success: #1fb999;
    --gray-50: #f8fafc;
    --gray-100: #f1f5f9;
    --gray-200: #e2e8f0;
    --gray-500: #64748b;
    --gray-600: #475569;
    --gray-800: #1e293b;
  }
  
  @keyframes hero-flow {
    0% { transform: translate3d(-18%, -12%, 0) scale(1); opacity: 0.82; }
    25% { transform: translate3d(10%, -4%, 0) scale(1.08); opacity: 0.95; }
    50% { transform: translate3d(18%, 14%, 0) scale(1.16); opacity: 1; }
    75% { transform: translate3d(-8%, 6%, 0) scale(1.08); opacity: 0.93; }
    100% { transform: translate3d(-18%, -12%, 0) scale(1); opacity: 0.82; }
  }

  @keyframes hero-gradient-pan {
    0% { background-position: 18% 24%, 82% 32%, 50% 50%; }
    50% { background-position: 36% 10%, 64% 62%, 52% 56%; }
    100% { background-position: 18% 24%, 82% 32%, 50% 50%; }
  }
  
  .hero-glow {
    position: absolute;
    inset: -30% -20% auto -20%;
    height: 120%;
    background: radial-gradient(circle at 70% 30%, rgba(255, 122, 89, 0.52), transparent 52%),
                radial-gradient(circle at 30% 65%, rgba(45, 27, 105, 0.48), transparent 55%),
                radial-gradient(circle at 50% 40%, rgba(255, 193, 149, 0.36), transparent 45%);
    filter: blur(72px);
    opacity: 0.95;
    animation: hero-flow 14s ease-in-out infinite;
    will-change: transform, opacity;
    pointer-events: none;
  }
  
  /* Hero Section */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 7.5rem 24px 4rem;
    background: radial-gradient(circle at 22% 24%, rgba(255, 122, 89, 0.28), transparent 42%),
                radial-gradient(circle at 78% 32%, rgba(45, 27, 105, 0.24), transparent 45%),
                linear-gradient(135deg, #fff7f2 0%, #fff2fb 50%, #f6f0ff 100%);
    background-size: 170% 170%, 170% 170%, 100% 100%;
    background-position: 18% 24%, 82% 32%, 50% 50%;
    animation: hero-gradient-pan 22s ease-in-out infinite;
    overflow: hidden;
  }
  
  .hero::before {
    content: '';
    position: absolute;
    inset: -30% -20% auto -20%;
    height: 120%;
    background: radial-gradient(circle at 70% 30%, rgba(255, 122, 89, 0.46), transparent 52%),
                radial-gradient(circle at 25% 70%, rgba(45, 27, 105, 0.44), transparent 55%);
    filter: blur(64px);
    opacity: 0.9;
    animation: hero-flow 18s ease-in-out infinite;
    animation-delay: -7s;
    will-change: transform, opacity;
    pointer-events: none;
  }
  
  .hero-container {
    max-width: 1180px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: var(--primary);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 2rem;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
  }
  
  .hero-title {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    line-height: 1.1;
    color: var(--gray-800);
    margin-bottom: 1.5rem;
  }
  
  .hero-title .accent {
    color: var(--accent);
  }
  
  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.2rem);
    color: var(--gray-600);
    line-height: 1.7;
    margin-bottom: 2rem;
    max-width: 540px;
  }
  
  .hero-stats {
    display: flex;
    gap: 2rem;
    margin-bottom: 2rem;
  }
  
  .stat-number {
    display: block;
    font-size: 1.75rem;
    font-weight: 800;
    color: var(--primary);
  }
  
  .stat-label {
    font-size: 0.85rem;
    color: var(--gray-500);
  }
  
  .btn-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    border-radius: 0.5rem;
    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;
    transition: all 0.2s;
    cursor: pointer;
  }
  
  .btn-primary {
    background: var(--accent);
    color: white;
  }
  
  .btn-primary:hover {
    background: var(--accent-hover);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(224, 120, 80, 0.4);
  }
  
  .btn-secondary {
    background: white;
    color: var(--primary);
    border: 2px solid var(--primary);
  }
  
  .btn-secondary:hover {
    background: var(--primary);
    color: white;
  }
  
  /* Hero Visual */
  .hero-visual {
    position: relative;
    background: white;
    border-radius: 1rem;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15);
    padding: 2rem;
  }
  
  .dashboard-preview {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  
  .preview-header {
    display: flex;
    gap: 0.5rem;
    margin-bottom: 1rem;
  }
  
  .preview-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
  
  .preview-dot.red { background: #ef4444; }
  .preview-dot.yellow { background: #f59e0b; }
  .preview-dot.green { background: #10b981; }
  
  .preview-content {
    background: var(--gray-100);
    border-radius: 0.5rem;
    padding: 1.5rem;
  }
  
  .preview-stat {
    display: flex;
    justify-content: space-between;
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--gray-200);
  }
  
  .preview-stat:last-child {
    border-bottom: none;
  }
  
  .stat-label-small {
    color: var(--gray-500);
    font-size: 0.9rem;
  }
  
  .stat-value {
    font-weight: 700;
    color: var(--gray-800);
  }
  
  .stat-value.success {
    color: var(--success);
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  
  .hero-floating-card {
    position: absolute;
    background: white;
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    font-weight: 600;
    animation: float 3s ease-in-out infinite;
  }
  
  .floating-card-1 {
    top: -20px;
    right: -6px;
    color: var(--success);
  }
  
  .floating-card-2 {
    bottom: 30px;
    left: -40px;
    color: var(--accent);
    animation-delay: 1.5s;
  }
  
  /* Sections */
  .section {
    padding: 6rem 2rem;
  }
  
  .section-container {
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .section-header {
    margin-bottom: 4rem;
    max-width: 600px;
  }
  
  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 800;
    color: var(--gray-800);
    margin-bottom: 1rem;
  }
  
  .section-subtitle {
    font-size: 1.1rem;
    color: var(--gray-600);
  }
  
  /* Features Grid */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  
  .feature-card {
    background: var(--gray-50);
    border-radius: 1rem;
    padding: 2rem;
    transition: all 0.3s;
  }
  
  .feature-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
  }
  
  .feature-icon {
    width: 48px;
    height: 48px;
    background: var(--primary);
    border-radius: 0.75rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
  }
  
  .feature-icon svg {
    width: 24px;
    height: 24px;
    color: white;
  }
  
  .feature-card h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--gray-800);
    margin-bottom: 0.75rem;
  }
  
  .feature-card p {
    color: var(--gray-600);
    font-size: 0.95rem;
    line-height: 1.6;
  }
  
  /* Stats Section */
  .stats-section {
    background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
    color: white;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 3rem;
    max-width: 900px;
    margin: 0 auto;
  }
  
  .stat-item {
    text-align: center;
  }
  
  .stat-number-large {
    font-size: 3rem;
    font-weight: 800;
    line-height: 1;
    margin-bottom: 0.5rem;
  }
  
  .stat-label-large {
    font-size: 0.95rem;
    opacity: 0.9;
  }
  
  /* Responsive */
  @media (max-width: 1024px) {
    .hero-container {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    
    .features-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .stats-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
  }
  
  @media (max-width: 640px) {
    .features-grid {
      grid-template-columns: 1fr;
    }
    
    .hero-stats {
      flex-direction: column;
      gap: 1rem;
    }
    
    .btn-group {
      flex-direction: column;
    }
  }
</style>

<!-- Hero Section -->
<section class="hero">
  <div class="hero-glow" aria-hidden="true"></div>
  <div class="hero-container">
    <!-- Left: Content -->
    <div>
      <div class="hero-badge">
        <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="8" cy="8" r="6"/>
        </svg>
        Multi-Tenant Platform
      </div>
      
      <h1 class="hero-title">
        Bouw moderne <span class="accent">e-commerce</span> op één krachtig platform
      </h1>
      
      <p class="hero-subtitle">
        Pagayo biedt alles wat je nodig hebt om multi-tenant e-commerce te schalen: van betalingen tot inventory management, alles op één plaats.
      </p>
      
      <div class="hero-stats">
        <div>
          <span class="stat-number">50+</span>
          <span class="stat-label">Actieve webshops</span>
        </div>
        <div>
          <span class="stat-number">99.9%</span>
          <span class="stat-label">Uptime</span>
        </div>
      </div>
      
      <div class="btn-group">
        <a href="https://beheer.pagayo.com" class="btn btn-primary">
          Start gratis
          <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M5 1l6 6-6 6"/>
          </svg>
        </a>
        <a href="/nl/contact" class="btn btn-secondary">
          Neem contact op
        </a>
      </div>
    </div>
    
    <!-- Right: Visual -->
    <div class="hero-visual">
      <div class="preview-header">
        <span class="preview-dot red"></span>
        <span class="preview-dot yellow"></span>
        <span class="preview-dot green"></span>
      </div>
      
      <div class="dashboard-preview">
        <div class="preview-content">
          <div class="preview-stat">
            <span class="stat-label-small">Totale omzet</span>
            <span class="stat-value success">€12.458</span>
          </div>
          <div class="preview-stat">
            <span class="stat-label-small">Orders vandaag</span>
            <span class="stat-value">42</span>
          </div>
          <div class="preview-stat">
            <span class="stat-label-small">Actieve producten</span>
            <span class="stat-value">128</span>
          </div>
        </div>
      </div>
      
      <!-- Floating cards -->
      <div class="hero-floating-card floating-card-1">
        <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M9 1v12M3 7l6 6 6-6"/>
        </svg>
        +23% omzet
      </div>
      
      <div class="hero-floating-card floating-card-2">
        <svg width="18" height="18" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="9" cy="9" r="8"/>
          <path d="M6 9l2 2 4-4"/>
        </svg>
        Order #4891
      </div>
    </div>
  </div>
</section>

<!-- Features Section -->
<section class="section">
  <div class="section-container">
    <div class="section-header">
      <h2 class="section-title">
        Alles wat je nodig hebt om te groeien
      </h2>
      <p class="section-subtitle">
        Een volledig geïntegreerd pakket voor moderne e-commerce. Van betalingen tot verzending, alles werkt naadloos samen.
      </p>
    </div>
    
    <div class="features-grid">
      <!-- Feature 1 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="4" y="6" width="16" height="12" rx="2"/>
            <line x1="4" y1="10" x2="20" y2="10"/>
          </svg>
        </div>
        <h3>Betalingen</h3>
        <p>Accepteer alle betaalmethoden: iDEAL, creditcard, Bancontact, en meer. Automatische afrekening en facturatie.</p>
      </div>
      
      <!-- Feature 2 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="4" y="4" width="16" height="16" rx="2"/>
            <path d="M4 10h16M10 4v16"/>
          </svg>
        </div>
        <h3>Multi-Tenant</h3>
        <p>Beheer meerdere webshops vanuit één platform. Elke tenant heeft zijn eigen data, styling en instellingen.</p>
      </div>
      
      <!-- Feature 3 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="8"/>
            <path d="M12 6v6l4 2"/>
          </svg>
        </div>
        <h3>Realtime Analytics</h3>
        <p>Krijg direct inzicht in verkoop, voorraad en klantgedrag. Exporteer rapporten naar Excel of PDF.</p>
      </div>
      
      <!-- Feature 4 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="6" y="6" width="12" height="12" rx="2"/>
            <path d="M6 10h12M10 6v12"/>
          </svg>
        </div>
        <h3>Inventory Management</h3>
        <p>Houd voorraad bij over meerdere locaties. Automatische alerts bij lage voorraad en eenvoudige bijvulling.</p>
      </div>
      
      <!-- Feature 5 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 12V7a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h7"/>
            <path d="M16 21l5-5M21 21l-5-5"/>
          </svg>
        </div>
        <h3>Verzending</h3>
        <p>Integratie met PostNL, DHL en andere verzenders. Automatische trackcodes en verzendlabels.</p>
      </div>
      
      <!-- Feature 6 -->
      <div class="feature-card">
        <div class="feature-icon">
          <svg width="24" height="24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="8" r="4"/>
            <path d="M6 21v-2a4 4 0 0 1 4-4h4a4 4 0 0 1 4 4v2"/>
          </svg>
        </div>
        <h3>Klantenbeheer</h3>
        <p>Centraal CRM systeem voor al je klanten. Bestelgeschiedenis, voorkeuren en loyaliteitsprogramma's.</p>
      </div>
    </div>
  </div>
</section>

<!-- Stats Section -->
<section class="section stats-section">
  <div class="section-container">
    <div class="stats-grid">
      <div class="stat-item">
        <div class="stat-number-large">50+</div>
        <div class="stat-label-large">Actieve webshops</div>
      </div>
      <div class="stat-item">
        <div class="stat-number-large">99.9%</div>
        <div class="stat-label-large">Historische uptime</div>
      </div>
      <div class="stat-item">
        <div class="stat-number-large">24/7</div>
        <div class="stat-label-large">Support beschikbaar</div>
      </div>
    </div>
  </div>
</section>

<!-- CTA Section -->
<section class="section">
  <div class="section-container" style="text-align: center; max-width: 800px;">
    <h2 class="section-title" style="margin-bottom: 1.5rem;">
      Klaar om te starten?
    </h2>
    <p class="section-subtitle" style="margin-bottom: 2rem;">
      Sluit je aan bij tientallen ondernemers die Pagayo gebruiken om hun online business te laten groeien.
    </p>
    <div class="btn-group" style="justify-content: center;">
      <a href="https://beheer.pagayo.com" class="btn btn-primary">
        Start gratis
        <svg width="14" height="14" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M5 1l6 6-6 6"/>
        </svg>
      </a>
      <a href="/nl/contact" class="btn btn-secondary">
        Neem contact op
      </a>
    </div>
  </div>
</section>

</MarketingLayout>
