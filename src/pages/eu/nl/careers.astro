---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import HeaderDynamic from '../../../components/HeaderDynamic.astro';
import FooterDynamic from '../../../components/FooterDynamic.astro';
import nlCountry from '../../../data/countries/nl.json';

const country = nlCountry;
---

<BaseLayout title="Werken bij Pagayo" description="Sluit je aan bij Pagayo en bouw mee aan het platform dat ondernemers direct live laat gaan." lang="nl">
	<HeaderDynamic country={country} />
	<main class="page-container">
		<section class="hero-main">
			<div class="hero__content">
				<p class="eyebrow">Careers</p>
				<h1>Bouw mee aan direct-verkopen voor iedereen</h1>
				<p class="lead">We bouwen een platform waar ondernemers binnen 30 seconden live gaan. Geen lange implementaties, wel miljoenen transacties per jaar. Kom bij een team dat shipping boven meetings zet.</p>
				<div class="hero__actions">
					<a class="btn btn-ghost" href="/eu/nl/contact">Vraag over een rol? Neem contact op</a>
				</div>
			</div>

			<div class="hero__panel">
				<div class="stat-card">
					<p class="stat-label">Transacties per maand</p>
					<p class="stat-value">2M+</p>
					<p class="stat-footnote">Elke push heeft directe impact</p>
				</div>
				<div class="stat-card">
					<p class="stat-label">Werkwijze</p>
					<p class="stat-value">Ship snel</p>
					<p class="stat-footnote">Code review → merge → live</p>
				</div>
				<div class="stat-card">
					<p class="stat-label">Team</p>
					<p class="stat-value">Remote-first</p>
					<p class="stat-footnote">Werk waar je het beste werkt</p>
				</div>
			</div>
		</section>

		<section class="grid">
			<div class="card"><h3>Missie</h3><p>Ondernemers verdienen tooling die meteen werkt. Wij leveren dat: van webshop tot POS tot verhuur.</p></div>
			<div class="card"><h3>Werkwijze</h3><p>Kleine teams, snelle releases, metrics boven hiërarchie. Je shipt, meet en verbetert.</p></div>
			<div class="card"><h3>Impact</h3><p>Elke push beïnvloedt betalingen, voorraad en boekingen van duizenden shops en hotels.</p></div>
		</section>

		<section class="openings">
			<h2>Open rollen</h2>
			<div class="roles">
				<article class="role"><div class="badge">Engineering</div><h3>Senior Full-Stack Engineer</h3><p>Next.js, Node, Prisma, Stripe. Bouw flows die self-service onboarding, payments en compliance samenbrengen.</p><div class="meta">Remote-first • 4-dagen-optie</div></article>
				<article class="role"><div class="badge">Product</div><h3>Product Manager</h3><p>Eigenaarschap over onboarding en checkout. Werk met data, geen eindeloze decks.</p><div class="meta">Remote • Direct toegang tot klanten</div></article>
				<article class="role"><div class="badge">Design</div><h3>Product Designer</h3><p>Stripe-kwaliteit UX voor web, kassa en verhuur. Ontwerp flows die zonder uitleg te snappen zijn.</p><div class="meta">Remote • Figma + prototype ownership</div></article>
			</div>
		</section>

		<section class="benefits">
			<h2>Wat je krijgt</h2>
			<ul>
				<li>Remote-first met budget voor focusspaces</li>
				<li>Ship-ritme: wekelijkse releases, weinig meetings</li>
				<li>Salaris + equity + 30 vrije dagen</li>
				<li>Hardware naar keuze en opleidingsbudget</li>
			</ul>
		</section>

		<section class="cta">
			<h2>Klaar om te starten?</h2>
			<p>Stuur je profiel of portfolio. We reageren binnen 3 werkdagen, zonder lange rondes.</p>
			<div class="cta-actions">
				<a class="btn btn-primary" href="mailto:careers@pagayo.com">Mail je profiel</a>
			</div>
		</section>
	</main>
	<style>
		.grid, .openings, .benefits, .cta { margin-top: 4rem; }
		.eyebrow { display: inline-flex; align-items: center; gap: 0.4rem; padding: 0.35rem 0.9rem; border: 1px solid var(--border); border-radius: 999px; letter-spacing: 0.08em; text-transform: uppercase; color: var(--text-secondary); font-size: 0.85rem; }
		.lead { color: var(--text-secondary); max-width: 760px; line-height: 1.6; }
		.btn { text-decoration: none; padding: 0.85rem 1.4rem; border-radius: 10px; font-weight: 700; border: 1px solid var(--border); display: inline-flex; align-items: center; justify-content: center; gap: 0.45rem; }
		.btn.btn-primary { background: linear-gradient(135deg, var(--accent) 0%, #00e077 100%); color: #06130d; }
		.btn.btn-ghost { color: var(--text); }
		.grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1rem; }
		.card { border: 1px solid var(--border); border-radius: 14px; padding: 1.4rem; background: var(--bg-secondary); box-shadow: 0 8px 24px rgba(0,0,0,0.18); }
		.openings h2, .benefits h2, .cta h2 { margin-bottom: 0.6rem; }
		.roles { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 1rem; }
		.role { border: 1px solid var(--border); border-radius: 14px; padding: 1.4rem; background: var(--bg-secondary); display: flex; flex-direction: column; gap: 0.5rem; box-shadow: 0 10px 30px rgba(0,0,0,0.22); }
		.badge { display: inline-flex; align-items: center; padding: 0.3rem 0.7rem; border-radius: 999px; background: rgba(0,255,136,0.12); color: var(--accent); font-weight: 700; font-size: 0.85rem; }
		.meta { color: var(--text-secondary); font-size: 0.95rem; }
		.benefits ul { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 0.6rem; padding-left: 1.1rem; color: var(--text-secondary); }
		.cta { border: 1px solid var(--border); border-radius: 16px; padding: 1.8rem; background: linear-gradient(135deg, rgba(0,255,136,0.08), rgba(0,0,0,0.9)); text-align: center; box-shadow: 0 12px 32px rgba(0,0,0,0.25); display: flex; flex-direction: column; gap: 0.8rem; }
		.cta-actions { display: flex; gap: 0.8rem; flex-wrap: wrap; justify-content: center; }
		
		@media (max-width: 768px) {
			.benefits ul { grid-template-columns: 1fr; }
			.cta-actions { flex-direction: column; width: 100%; }
			.cta-actions .btn { width: 100%; }
		}
		
		@media (max-width: 480px) {
			.card { padding: 1.2rem; }
			.role { padding: 1.2rem; }
			.badge { font-size: 0.75rem; padding: 0.25rem 0.6rem; }
			.cta { padding: 1.5rem 1rem; }
		}
	</style>
        <FooterDynamic country={country} />
