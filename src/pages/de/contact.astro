---
import MarketingLayout from '../../layouts/MarketingLayout.astro';
import Button from '../../components/ui/Button.astro';
---

<MarketingLayout
  title="Kontakt - Pagayo Multi-Tenant SaaS Plattform"
  description="Nehmen Sie Kontakt mit unserem Team auf. Wir helfen Ihnen gerne bei Fragen zu unserer Multi-Tenant-SaaS-Lösung."
  lang="de"
>
  <!-- Hero Section -->
  <section class="container mx-auto px-6 py-20">
    <div class="max-w-3xl mx-auto text-center">
      <h1 class="text-5xl font-bold text-primary-dark mb-6">Kontakt aufnehmen</h1>
      <p class="text-xl text-gray-700 leading-relaxed">
        Fragen zu Pagayo? Unser Team steht bereit, Ihnen zu helfen.
      </p>
    </div>
  </section>

  <!-- Contact Methods Grid -->
  <section class="container mx-auto px-6 pb-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
      <!-- Email -->
      <div class="bg-white rounded-lg p-6 shadow-soft text-center">
        <div class="text-4xl mb-4">📧</div>
        <h3 class="text-xl font-bold mb-2">E-Mail</h3>
        <p class="text-gray-600 mb-4">Meist innerhalb von 24 Stunden</p>
        <a
          href="mailto:hello@pagayo.com"
          class="text-primary-purple font-medium hover:underline"
        >
          hello@pagayo.com
        </a>
      </div>

      <!-- Phone -->
      <div class="bg-white rounded-lg p-6 shadow-soft text-center">
        <div class="text-4xl mb-4">📞</div>
        <h3 class="text-xl font-bold mb-2">Telefon</h3>
        <p class="text-gray-600 mb-4">Mo-Fr 09:00-17:00 CET</p>
        <a href="tel:+31205550123" class="text-primary-purple font-medium hover:underline">
          +31 20 555 0123
        </a>
      </div>

      <!-- Live Chat -->
      <div class="bg-white rounded-lg p-6 shadow-soft text-center">
        <div class="text-4xl mb-4">💬</div>
        <h3 class="text-xl font-bold mb-2">Live-Chat</h3>
        <p class="text-gray-600 mb-4">Direkte Hilfe über unseren Chat</p>
        <button class="text-primary-purple font-medium hover:underline" onclick="openChat()">
          Chat starten
        </button>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="bg-gray-50 py-20">
    <div class="container mx-auto px-6">
      <div class="max-w-2xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">Senden Sie uns eine Nachricht</h2>
        <form
          id="contact-form"
          class="bg-white rounded-lg shadow-soft p-8"
          action="https://beheer.pagayo.com/api/contact"
          method="POST"
        >
          <div class="mb-6">
            <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
              Name *
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-purple focus:border-transparent"
              placeholder="Ihr Name"
            />
          </div>

          <div class="mb-6">
            <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
              E-Mail *
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-purple focus:border-transparent"
              placeholder="ihre@email.de"
            />
          </div>

          <div class="mb-6">
            <label for="company" class="block text-sm font-medium text-gray-700 mb-2">
              Unternehmen
            </label>
            <input
              type="text"
              id="company"
              name="company"
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-purple focus:border-transparent"
              placeholder="Ihr Unternehmen (optional)"
            />
          </div>

          <div class="mb-6">
            <label for="subject" class="block text-sm font-medium text-gray-700 mb-2">
              Betreff *
            </label>
            <select
              id="subject"
              name="subject"
              required
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-purple focus:border-transparent"
            >
              <option value="">Wählen Sie einen Betreff</option>
              <option value="sales">Vertrieb & Preise</option>
              <option value="demo">Demo anfordern</option>
              <option value="technical">Technische Frage</option>
              <option value="support">Support</option>
              <option value="partnership">Partnerschaft</option>
              <option value="other">Sonstiges</option>
            </select>
          </div>

          <div class="mb-6">
            <label for="message" class="block text-sm font-medium text-gray-700 mb-2">
              Nachricht *
            </label>
            <textarea
              id="message"
              name="message"
              required
              rows="6"
              class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-purple focus:border-transparent resize-none"
              placeholder="Erzählen Sie uns, wie wir Ihnen helfen können..."
            ></textarea>
          </div>

          <input type="hidden" name="language" value="de" />
          <input type="hidden" name="source" value="marketing_website" />

          <Button variant="primary" type="submit" class="w-full">
            Nachricht senden →
          </Button>

          <p class="text-sm text-gray-600 text-center mt-4">
            Mit dem Absenden dieses Formulars stimmen Sie unserer
            <a href="/de/datenschutz" class="text-primary-purple hover:underline"
              >Datenschutzerklärung</a
            > zu.
          </p>
        </form>

        <div id="form-success" class="hidden bg-green-50 border border-green-200 rounded-lg p-6 mt-6">
          <h3 class="text-green-800 font-bold mb-2">✓ Nachricht gesendet!</h3>
          <p class="text-green-700">
            Vielen Dank für Ihre Nachricht. Wir melden uns innerhalb von 24 Stunden.
          </p>
        </div>

        <div id="form-error" class="hidden bg-red-50 border border-red-200 rounded-lg p-6 mt-6">
          <h3 class="text-red-800 font-bold mb-2">✗ Etwas ist schief gelaufen</h3>
          <p class="text-red-700">
            Bitte versuchen Sie es später erneut oder senden Sie eine E-Mail an hello@pagayo.com
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Location Info -->
  <section class="container mx-auto px-6 py-20">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-3xl font-bold text-center mb-12">Unser Büro</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
        <div>
          <h3 class="text-xl font-bold mb-4">📍 Amsterdam, Niederlande</h3>
          <p class="text-gray-700 mb-2">Pagayo B.V.</p>
          <p class="text-gray-700 mb-2">Herengracht 123</p>
          <p class="text-gray-700 mb-4">1015 BD Amsterdam</p>
          <p class="text-gray-700 mb-2">KVK: 12345678</p>
          <p class="text-gray-700">USt-ID: NL123456789B01</p>
        </div>
        <div class="bg-gray-200 rounded-lg h-64 flex items-center justify-center">
          <p class="text-gray-600">Google Maps Platzhalter</p>
        </div>
      </div>
    </div>
  </section>

  <!-- FAQ Section -->
  <section class="bg-gray-50 py-20">
    <div class="container mx-auto px-6">
      <div class="max-w-3xl mx-auto">
        <h2 class="text-3xl font-bold text-center mb-12">Häufig gestellte Fragen</h2>
        <div class="space-y-6">
          <div class="bg-white rounded-lg p-6 shadow-soft">
            <h3 class="text-lg font-bold mb-2">Wie schnell bekomme ich eine Antwort?</h3>
            <p class="text-gray-700">
              Wir bemühen uns, alle Anfragen innerhalb von 24 Stunden zu beantworten. Für dringende
              Angelegenheiten können Sie uns während der Geschäftszeiten anrufen.
            </p>
          </div>
          <div class="bg-white rounded-lg p-6 shadow-soft">
            <h3 class="text-lg font-bold mb-2">Kann ich eine Demo anfordern?</h3>
            <p class="text-gray-700">
              Ja! Wählen Sie "Demo anfordern" im Formular und wir vereinbaren innerhalb von 2
              Werktagen eine persönliche Demo.
            </p>
          </div>
          <div class="bg-white rounded-lg p-6 shadow-soft">
            <h3 class="text-lg font-bold mb-2">Bieten Sie Support auf Deutsch an?</h3>
            <p class="text-gray-700">
              Absolut. Unser Team spricht Niederländisch, Englisch und Deutsch. Support ist per
              E-Mail, Chat und Telefon verfügbar.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</MarketingLayout>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const successMsg = document.getElementById('form-success');
  const errorMsg = document.getElementById('form-error');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(form);
    const data = Object.fromEntries(formData);

    try {
      const response = await fetch(form.action, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      if (response.ok) {
        form.classList.add('hidden');
        successMsg?.classList.remove('hidden');
        errorMsg?.classList.add('hidden');
      } else {
        throw new Error('Server error');
      }
    } catch (error) {
      errorMsg?.classList.remove('hidden');
      successMsg?.classList.add('hidden');
    }
  });

  function openChat() {
    alert('Live-Chat wird bald hinzugefügt!');
  }
</script>
