# Pagayo packages via GitHub Package Registry
@pagayo:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Keystatic has peer dep on astro "2 || 3 || 4 || 5" but we're on Astro 6
# Remove this when @keystatic/astro adds Astro 6 support
legacy-peer-deps=true
